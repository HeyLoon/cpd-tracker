# 🔧 修復 GitHub Pages 部署錯誤

## 錯誤訊息
```
Error: Failed to create deployment (status: 404)
Ensure GitHub Pages has been enabled
```

## 原因
GitHub Pages 尚未啟用，所以 Actions 無法部署。

---

## ✅ 解決步驟（按順序執行）

### 步驟 1：啟用 GitHub Pages

1. 前往你的 Repository：
   ```
   https://github.com/HeyLoon/cpd-tracker/settings/pages
   ```

2. 在 **Source** 區域：
   - 選擇 **GitHub Actions**（不是 Branch）
   
3. **不需要** 點擊 Save（選擇後自動生效）

4. 頁面應該會顯示類似：
   ```
   Your site is ready to be published at 
   https://heyloon.github.io/cpd-tracker/
   ```

---

### 步驟 2：重新執行 Actions

1. 前往 Actions 標籤：
   ```
   https://github.com/HeyLoon/cpd-tracker/actions
   ```

2. 找到失敗的 workflow（紅色 ❌）

3. 點擊進入 → 點擊右上角 **Re-run all jobs**

---

### 步驟 3：等待部署完成

- 應該會看到：
  - ✅ build job 成功
  - ✅ deploy job 成功
  
- 大約 2-3 分鐘完成

---

### 步驟 4：訪問你的應用程式

```
https://heyloon.github.io/cpd-tracker/#/
```

**注意：** 網址中的 `#/` 是必要的！

---

## 🔐 關於 .env 安全性（已修復）

我已經：

1. ✅ 將 `.env` 從 Git 移除
2. ✅ 加入 `.gitignore` 防止未來誤提交
3. ✅ 修改 GitHub Actions 使用 Secrets（未來填入時生效）

### 未來設定 PocketBase URL（可選）

當後端部署完成後：

1. 前往 Repository → **Settings** → **Secrets and variables** → **Actions**

2. 點擊 **New repository secret**

3. 填入：
   - **Name**: `VITE_POCKETBASE_URL`
   - **Value**: `https://your-api-domain.com`

4. 重新推送程式碼，Actions 會自動使用新的 URL

### 本地開發時

手動建立 `.env` 檔案（不會提交到 Git）：

```bash
# 複製範例檔案
cp .env.example .env

# 編輯填入你的 URL
# VITE_POCKETBASE_URL=http://192.168.1.100:8090
```

---

## ✅ 檢查清單

- [ ] 前往 Settings → Pages 啟用 GitHub Actions
- [ ] 重新執行失敗的 workflow
- [ ] 等待綠色勾勾 ✅
- [ ] 開啟 `https://heyloon.github.io/cpd-tracker/#/`
- [ ] 測試新增資產功能
- [ ] 測試離線功能（關閉網路）

---

## 🎯 為什麼會有這個錯誤？

GitHub Pages 有兩種啟用方式：

### 方式 1：Branch 方式（舊）
- 從 `gh-pages` 分支部署
- 需要手動建立分支
- 需要額外設定

### 方式 2：GitHub Actions 方式（新，我們用的）✅
- 從 Actions workflow 直接部署
- 更靈活、更強大
- **但必須先在 Settings 啟用**

你遇到的錯誤是因為還沒啟用，不是程式碼問題！

---

## 📸 截圖參考

### Settings → Pages 應該長這樣：

```
┌─────────────────────────────────────┐
│ Build and deployment                │
│                                     │
│ Source:  [GitHub Actions ▼]        │
│                                     │
│ ✅ Your site is ready to be        │
│    published at:                   │
│    https://heyloon.github.io/...   │
└─────────────────────────────────────┘
```

---

完成後告訴我結果！🚀
